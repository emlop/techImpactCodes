// canvas and background defined
function setup() {
  createCanvas(600, 400);
  background(150, 150, 100);
}

function draw() {

  fill(200, 100, 250); // filled color of ellipse
  ellipse(mouseX, mouseY, 100, 100); // created an ellipse
  
  function mousePressed() { // when mousePressed ellipse spreads across the canvas

  }
}
